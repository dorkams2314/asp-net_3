@model IEnumerable<asp_net_3.Models.CartItem>

@{
    ViewData["Title"] = "Товары в корзине";
}

@await Html.PartialAsync("_AdminMenu")

<div class="page-block">
    <div class="list-header">
        <h1 class="page-title">Товары в корзине</h1>
        <a class="btn btn-primary" asp-action="Create">Добавить запись</a>
    </div>

    <table class="simple-table">
        <thead>
            <tr>
                <th>Id</th>
                <th>CartId</th>
                <th>ProductId</th>
                <th>Товар</th>
                <th>Количество</th>
                <th>Действия</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var item in Model) {
            <tr>
                <td>@item.Id</td>
                <td>@item.CartId</td>
                <td>@item.ProductId</td>
                <td>@item.Product?.Name</td>
                <td>@item.Quantity</td>
                <td class="simple-actions">
                    <a class="btn btn-outline-secondary btn-sm" asp-action="Edit" asp-route-id="@item.Id">Изменить</a>
                    <a class="btn btn-outline-danger btn-sm" asp-action="Delete" asp-route-id="@item.Id">Удалить</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>
