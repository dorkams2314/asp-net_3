@model asp_net_3.Models.OrderItem

@{
    ViewData["Title"] = "Удалить запись состава заказа";
}

@await Html.PartialAsync("_AdminMenu")

<div class="page-block">
    <h1 class="page-title">Удалить запись состава заказа</h1>
    <p>Вы действительно хотите удалить эту запись?</p>

    @if (TempData["DeleteError"] != null) {
        <div class="alert alert-danger">@TempData["DeleteError"]</div>
    }

    <table class="simple-table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>@Model.Id</td>
            </tr>
            <tr>
                <th>OrderId</th>
                <td>@Model.OrderId</td>
            </tr>
            <tr>
                <th>Товар</th>
                <td>@Model.Product?.Name</td>
            </tr>
            <tr>
                <th>Количество</th>
                <td>@Model.Quantity</td>
            </tr>
            <tr>
                <th>Цена</th>
                <td>@Model.Price.ToString("N2")</td>
            </tr>
        </tbody>
    </table>

    <form asp-action="Delete" method="post" class="mt-3">
        <input asp-for="Id" type="hidden" />
        <button type="submit" class="btn btn-danger">Удалить</button>
        <a class="btn btn-outline-secondary" asp-action="Index">Отмена</a>
    </form>
</div>
